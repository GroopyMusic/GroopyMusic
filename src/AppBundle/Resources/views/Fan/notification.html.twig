{% extends "base.html.twig" %}

{% block body %}
    <h2>
        {{ notif.title }}
    </h2>

    <div>On {{ notif.created|date('d/m/Y') }}</div>

    <p>
        {{ notif.content }}
    </p>
{% endblock %}