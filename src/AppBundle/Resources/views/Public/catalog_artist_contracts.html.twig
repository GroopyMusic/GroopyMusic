{% extends "base.html.twig" %}

{% block body %}
    {% if current_contracts is empty and successful_contracts is empty %}

        <div class="container">
            <h3 class="text-center mb-3">Evénements Un-Mute</h3>
            <p class="p-info">
                Il n'y a aucun événement à soutenir en ce moment. Revenez très vite pour des nouveautés et tenez-vous au courant grâce à
                <a href="https://www.facebook.com/UnMuteasbl/">notre page sur Facebook.</a>
            </p>
            <p class="text-center">
                Si vous êtes un artiste et que vous souhaitez organiser un concert grâce à Un-Mute, <a href="{{ path('suggestionBox') }}">contactez-nous</a>.
            </p>
        </div>

    {% else %}
        {% if current_contracts is not empty %}
            <div class="container">
                <h3 class="text-center mb-3">Soutenez-les !</h3>

                <p>
                    Les événements suivants ont encore besoin de soutien pour pouvoir être produits !
                    Offrez à ces artistes la possibilité de se produire dans une salle comble en achetant des tickets pour leurs concerts.
                </p>

                <div class="row">
                    {% for contract in current_contracts %}
                        {% include 'AppBundle:Public/Card:contract_artist.html.twig' with {'contract': contract, 'bgwhite': true} %}
                    {% endfor %}
                </div>
            </div>
        {% endif %}
        {% if successful_contracts is not empty %}
            <div class="container{% if current_contracts is not empty %} mt-4{% endif %}">
                <h3 class="text-center mb-3">Ils sont confirmés</h3>
                <div class="row">
                    {% for contract in successful_contracts %}
                        {% include 'AppBundle:Public/Card:contract_artist.html.twig' with {'contract': contract, 'bgwhite': true} %}
                    {% endfor %}
                </div>
            </div>
        {% endif %}
    {% endif %}
{% endblock %}