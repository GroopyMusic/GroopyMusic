{% extends "base.html.twig" %}

{% block body %}
    {% if current_contracts is not empty %}
        <div class="container">
            <h3 class="rounded-title">Soutenez-les !</h3>
            <div class="row">
        {% for contract in current_contracts %}
            {% include 'AppBundle:Public/Card:contract_artist.html.twig' with {'contract': contract} %}
        {% endfor %}
            </div>
        </div>
    {% endif %}

    {% if successful_contracts is not empty %}
        <div class="container">
        <h3 class="rounded-title">Ils sont confirmés</h3>
            <div class="row">
        {% for contract in successful_contracts %}
            {% include 'AppBundle:Public/Card:contract_artist.html.twig' with {'contract': contract} %}
        {% endfor %}
    {% endif %}
{% endblock %}