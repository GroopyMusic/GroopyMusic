{% extends "AppBundle:Notifications:base.html.twig" %}

{% trans_default_domain 'notifications' %}

{% set title = 'successful_contract_artist.title'|trans|raw %}
{%- set content -%}
   {{ 'successful_contract_artist.content'|trans({
       '%event_url%': url('artist_contract', {'id':contract_id }),
       '%step%': step,
       '%date%': date,
       '%hall_url%': url('hall', {'id': hall_id}),
       '%hall_name%': hall_name,
   })|raw|nl2br  }}
{%- endset -%}