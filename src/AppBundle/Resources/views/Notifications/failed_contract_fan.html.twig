{% extends "AppBundle:Notifications:base.html.twig" %}

{% trans_default_domain 'notifications' %}

{% set title = 'failed_contract_fan.title'|trans({'%artist%': artist})|raw %}
{%- set content -%}
    {{ 'failed_contract_fan.content'|trans({
        '%artist%': artist,
        '%event_url%': path('artist_contract', {'id': contract_id}),
        '%events_url%':path('catalog_crowdfundings'),
        '%step%': step
    })|raw|nl2br }}
{%- endset -%}