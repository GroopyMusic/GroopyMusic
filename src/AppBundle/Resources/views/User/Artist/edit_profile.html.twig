{% extends "AppBundle:Bases:base_artist.html.twig" %}

{% block body %}

    <h1>Edition du profil de l'artiste {{ artist.artistname }}</h1>

    <div>
        <a href="{{ path('artist_owners', {'id': artist.id}) }}">Propriétaires</a> <br />
        <a href="{{ path('artist_edit_photos', {'id': artist.id}) }}">Photos</a>
    </div>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_rest(form) }}
{% endblock  %}
