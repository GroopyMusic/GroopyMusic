{% set active_link = 'advanced' %}

{% extends "FOSUserBundle:Profile:layout.html.twig" %}

{% form_theme form ':Form:bootstrap_4_layout.html.twig' %}

{% block user_profile_content %}
    <div class="modal fade" id="signOutModal" tabindex="-1" role="dialog" aria-labelledby="signOutModalLabel" aria-hidden="true">>
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-body">
                    <p>Attention, cette action est irréversible !</p>
                </div>
                <div class="modal-footer">
                    {{ form_start(form) }}
                        {{ form_widget(form.submit) }}
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Annuler</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

    <h3>Options avancées</h3>

    <p>En quittant la plateforme, blablabla... Vous ne serez plus repris dans les propriétaires des artistes dont vous faites actuellement partie des gestionnaires et n'aurez donc plus accès à leur gestion.</p>

    <input id="checkbox-suppress-account" class="mr-3" type="checkbox" data-toggle="collapse" data-target="#collapse-suppress-account"/><label for="checkbox-suppress-account">Je souhaite supprimer mon compte</label>

    <div class="collapse" id="collapse-suppress-account">
        <button id="button-suppress-account" type="button" class="btn btn-danger" data-toggle="modal" data-target="#signOutModal">
            Supprimer mon compte
        </button>
    </div>


{% endblock %}