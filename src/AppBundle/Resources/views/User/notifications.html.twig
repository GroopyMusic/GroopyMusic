{% extends "base.html.twig" %}

{% block body %}
    <table>
        {% for notif in notifs %}
        <tr>
            <td>
                {{ preview_notification(notif)|raw }}
            </td>
        </tr>

        {% else %}
           <tr>
               <td>
                   Il n'y a pas encore de notification pour l'instant
               </td>
           </tr>
        {% endfor %}
    </table>
{% endblock %}