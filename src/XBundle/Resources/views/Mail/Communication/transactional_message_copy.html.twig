{% extends 'XBundle:Mail:baseEmailLayout.html.twig' %}

{% autoescape false %}
    {% spaceless %}
        {% block title %}{{ subject }}{% endblock %}

        {% block content %}
            {{ email_content('x.transactional_message_copy', {
                '%date%': message.date|date('d/m/Y \\à H:i'),
                '%project%': email_link(url('x_project', {'id': project.id, 'slug': project.slug}), project.title),
                '%title%': message.title,
                '%content%': message.content|nl2br,
            })|raw }}
        {% endblock %}
    {% endspaceless %}
{% endautoescape %}